---
source_pdf: Lesson 7 (2024秋季)-BeibeiWang.pdf
part: 第7章
keywords: search, sequential-search, binary-search, BST, ASL
---

# 静态搜索与二叉搜索树（★★★）

#data-structure #search #bst #tree #time-complexity #concept

## 概览表（一目了然）
| 条目 | 要点 |
|------|------|
| **搜索** | 在数据集合中寻找满足条件的数据对象 |
| **关键码** | 唯一标识对象的属性域 |
| **静态搜索表** | 结构不变；顺序搜索 O(n)、折半搜索 O(log₂n) |
| **动态搜索表** | 结构随插入/删除调整（BST、AVL） |
| **ASL** | 平均搜索长度 = Σpᵢ·cᵢ |
| **BST** | 左 < 根 < 右；中序遍历有序 |

## 搜索基本概念

**搜索**：在数据集合中寻找**关键码**等于给定值的数据对象。

| 术语 | 含义 |
|------|------|
| **搜索结构** | 用于搜索的数据集合 |
| **关键码（Key）** | 唯一标识对象的属性 |
| **搜索成功** | 返回对象位置/信息 |
| **搜索失败** | 返回失败标志 |

### 两种搜索环境

| 类型 | 特点 | 代表结构 |
|------|------|----------|
| **静态搜索表** | 插入/删除前后结构不变 | 有序数组 |
| **动态搜索表** | 操作后自动调整结构 | BST、AVL |

---

## 顺序搜索

从表头开始逐一比较，直到找到或遍历完毕。

**监视哨技巧**：将目标值 x 放在表末尾（`Element[n].key = x`），省去越界检查。

```
SeqSearch(x):
  Element[CurrentSize].key = x  // 监视哨
  i = 0
  while Element[i].key ≠ x: i++
  return i + 1
```

### ASL 分析

| 情形 | ASL |
|------|-----|
| **等概率成功** | ASL_succ = (n+1)/2 |
| **搜索失败** | ASL_unsucc = n+1 |
| **时间复杂度** | **O(n)** |

---

## 折半搜索（二分搜索）

**前提**：有序顺序表。每次与中间元素比较，缩小一半搜索范围。

```
BinarySearch(x):
  low = 0, high = n-1
  while low ≤ high:
    mid = ⌊(low+high)/2⌋
    if x == Element[mid].key: return mid  // 成功
    if x < Element[mid].key: high = mid-1  // 左半
    else: low = mid+1                      // 右半
  return -1  // 失败
```

### 判定树与 ASL

折半搜索过程对应一棵**判定树**（二叉树），第 k 层的结点需比较 k 次。

| 情形 | 公式 | 示例（n=6） |
|------|------|-------------|
| **成功** | ASL_succ ≈ log₂(n+1) - 1 | 14/6 ≈ 2.33 |
| **失败** | ASL_unsucc 由外部结点层次决定 | 20/7 ≈ 2.86 |
| **时间复杂度** | **O(log₂n)** | — |

> [!warning] 折半搜索的局限
> 仅适用于**有序顺序表**（数组），不适用于链表（无法随机访问）。

---

## 二叉搜索树（BST）

**定义**：二叉搜索树或为空树，或满足：
1. 每个结点有唯一关键码
2. **左子树**所有关键码 **<** 根结点关键码
3. **右子树**所有关键码 **>** 根结点关键码
4. 左右子树也是 BST

**核心性质**：BST 的**中序遍历**产生**升序序列**（故又称二叉排序树）。

### BST 搜索

```
Search(x, ptr):
  if ptr == NULL: return NULL      // 失败
  if x < ptr.key: Search(x, ptr.left)
  if x > ptr.key: Search(x, ptr.right)
  return ptr                        // 成功
```

- 沿根到叶路径搜索，最多比较次数 = **树的高度 h**

### BST 插入

先搜索确认不存在，再在搜索失败位置插入新叶结点。

```
Insert(e1, ptr):
  if ptr == NULL: ptr = new Node(e1)  // 作为叶结点插入
  else if e1 < ptr.key: Insert(e1, ptr.left)
  else if e1 > ptr.key: Insert(e1, ptr.right)
  else: return false  // 已存在
```

> [!important] 插入顺序影响树形
> 同一组关键码，不同插入顺序产生不同 BST，高度可能差异很大。

### BST 删除（三种情况）

| 情况 | 处理方法 |
|------|----------|
| **叶结点** | 直接删除，父指针置 NULL |
| **仅有一个子树** | 用子树根顶替被删结点 |
| **有两个子树** | 用**右子树中序第一个结点**（最小值）替换，再删除该结点 |

```
              53                    53
             /  \     删除45       /  \
           17    78   ------>    17    78
          /  \                 /  \
         9   45               9   23
            /
           23
```

### BST 性能分析

| 情形 | ASL | 说明 |
|------|-----|------|
| **最好**（平衡） | O(log₂n) | 类似折半搜索 |
| **最坏**（退化为链） | O(n) | 有序插入时 |
| **平均** | O(log₂n) | 随机插入 |

---

## 搜索方法综合比较

| 方法 | 时间复杂度 | 数据要求 | 适用场景 |
|------|-----------|----------|----------|
| **顺序搜索** | O(n) | 无要求 | 小规模/无序数据 |
| **折半搜索** | O(log₂n) | 有序数组 | 静态有序表 |
| **BST 搜索** | O(log₂n)~O(n) | BST 结构 | 动态数据集 |

---

## 考试/测试常见模式
| 场景/关键词 | 答案 |
|-------------|------|
| "ASL_succ 等概率顺序搜索" | **(n+1)/2** |
| "折半搜索判定树" | **画出对应二叉树，各层比较次数** |
| "给定序列建BST" | **依次插入，每个新结点是叶结点** |
| "BST删除有两个子女的结点" | **用右子树中序第一个结点替换** |
| "BST最坏情况" | **有序插入退化为链表，O(n)** |
| "中序遍历BST" | **得到升序序列** |

## 相关笔记
- [[AVL树]]
- [[字典与散列]]
- [[树的基本概念与二叉树]]
- [[二叉树遍历与线索化]]
- [[数据结构基本概念与算法分析]]
