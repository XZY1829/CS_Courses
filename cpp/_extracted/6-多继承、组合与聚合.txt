
============================================================
=== Slide 1 ===
============================================================

3.5  多继承
智能科学与技术学院
任宇翔

============================================================
=== Slide 2 ===
============================================================

多继承的需要性
对于下面的两个类A和B：
class A
{		int m;
public:
void fa();
};
class B
{		int n;
public:
void fb();
};
如何定义一个类C，它包含A和B的所有成员，另外还拥有新的数据成员r和成员函数fc？

============================================================
=== Slide 3 ===
============================================================

多继承的需要性
方法一：用单继承实现
class C: public A //让C从A继承
{		int n,r; //把B类中的n复制过来
public:
void fb(); //把B类中的fb复制过来
void fc();
};
或者
class C: public B //让C从B继承
{		int m,r; //把A类中的m复制过来
public:
void fa(); //把A类中的fa复制过来
void fc();
};
不足之处：
概念混乱：导致A和B之间增加了层次关系。
易造成不一致：A中的m、fa（或B中的n、fb）与C中的m、fa（或n、fb）。
不能完全实现子类型：只有A和B中某一类对象能被C类对象替代。

============================================================
=== Slide 4 ===
============================================================

多继承的需要性
方法二：用成员对象实现
class C
{		A a;
B b;
int r;
public:
void fa() { a.fa(); }  //重新实现fa
void fb() { b.fb(); }  //重新实现fb
void fc();
};
不足之处：
不能实现子类型：程序中需要A和B类对象的地方都不能用C类对象去替代。

============================================================
=== Slide 5 ===
============================================================

多继承的需要性
用多继承实现
class C: public A, public B
{		int r;
public:
void fc();
};
多继承增强了语言的表达能力，它使得语言能够自然、方便地描述问题领域中的存在于对象类之间的多继承关系。

============================================================
=== Slide 6 ===
============================================================

多继承的类定义
多继承是指派生类可以有一个以上的直接基类。多继承的派生类定义格式为：
class <派生类名>: [<继承方式>] <基类名1>,
[<继承方式>] <基类名2>, …
{	<成员说明表>
};
继承方式及访问控制的规定同单继承。
派生类拥有所有基类的所有成员。
基类的声明次序决定派生类对象：
对基类数据成员的存储安排。
对基类构造函数/析构函数的调用次序。

============================================================
=== Slide 7 ===
============================================================

内存布局与构造次序
构造函数的执行次序是：
A()、B()、C()
（A()和B()实际是在C()的成员初始化表中调用的！）
对象c的内存空间布局是：   	        c
A::m
B::n
C::r
class C: public A, public B
{		int r;
public:
C();
void fc();
};
......
C c;
c.fa(); //OK
c.fb(); //OK
c.fc(); //OK

============================================================
=== Slide 8 ===
============================================================

内存布局与构造次序
public多继承的派生类与它的各个基类之间是子类型关系：
各个基类的指针或引用可以指向或引用派生类对象
注意：把多继承派生类对象的地址赋给基类的指针时会自动进行地址调整。例如，对于前面的三个类：
C c;
A *pa=&c;                   c.fa();-->A::fa(this);
B *pb=&c;                    c.fb();-->B::fb(this);
c
A::m
B::n
C::r
通过pa只能访问到A类和C类中的内容，但不会访问到B类中的内容。
通过pb只能访问到B类和C类中的内容，但不会访问到A类中的内容。
通过pa或pb访问C类中的内容一般要通过虚函数动态绑定来实现。

============================================================
=== Slide 9 ===
============================================================

多继承带来的问题
多继承会使得：
语言特征复杂化
编译程序的难度加大
消息绑定复杂化
从而给正确使用多继承带来困难
因此，有些面向对象语言（如Smalltalk、Java等）放弃了多继承机制。
多继承带来的两个主要问题：
名冲突问题
重复继承问题

============================================================
=== Slide 10 ===
============================================================

名冲突问题

============================================================
=== Slide 11 ===
============================================================

名冲突问题
C++解决名冲突的办法是：基类名受限
class C: public A, public B
{		......
public:
void func()
{	A::f(); //OK，调用A的f。
B::f(); //OK，调用B的f。
}
};
......
C c;
c.A::f(); //OK，调用A的f。
c.B::f(); //OK，调用B的f。

============================================================
=== Slide 12 ===
============================================================

重复继承问题
下面的类D从类A继承两次，称为重复继承：
class A
{  int x;
......
};
class B: public A { ...... };
class C: public A { ...... };
class D: public B, public C { ...... };
上述D类的对象d将包含两个x：
D d; //d包含B::x和C::x

============================================================
=== Slide 13 ===
============================================================

虚基类
如果要求类D中只有一个x，则应把A定义为B和C的虚基类：
class A { int x; ......};
class B: virtual public A {......};
class C: virtual public A {......};
class D: public B, public C {......};
在多继承中，如果有公共的虚基类（直接或间接），那么，这些虚基类的数据成员在派生类中将会被合并！
这样，上述D类的对象d就只有一个x了：
D d; //d只有A::x
A
C
D
B

============================================================
=== Slide 14 ===
============================================================

虚基类构造函数的调用
对于直接或间接拥有虚基类的类，创建该类的对象时：
虚基类的构造函数由该类的构造函数直接调用。
虚基类的构造函数优先非虚基类的构造函数执行。

============================================================
=== Slide 15 ===
============================================================

虚基类构造函数的调用
class A
{  int x;
public:
A(int i) { x = i; }
};
class B: virtual public A //包含虚基类A
{  int y;
public:
B(int i): A(1) { y = i; }
};
class C: virtual public A //包含虚基类A
{  int z;
public:
C(int i): A(2) { z = i; }
};

============================================================
=== Slide 16 ===
============================================================

虚基类构造函数的调用
class D: public B, public C //包含公共的虚基类A
{  int m;
public:
D(int i, int j, int k): B(i), C(j), A(3) { m = k; }
};
class E: public D //包含公共的虚基类A
{  int n;
public:
E(int i, int j, int k, int l): D(i,j,k), A(4) { n = l; }
};
......
D d(1,2,3);  //这里，A的构造函数由D调用，d.x初始化为3
调用的构造函数及它们的执行次序是：
A(3)、B(1)、C(2)、D(1,2,3)
E e(1,2,3,4);  //这里， A的构造函数由E调用，e.x初始化为4
调用的构造函数及它们的执行次序是：
A(4)、B(1)、C(2)、D(1,2,3)、E(1,2,3,4)

============================================================
=== Slide 17 ===
============================================================

虚基类的一种实现
class A
{  int x;
};
class B:  public A
{  int y;
void fb() { ...x... } //this->x
};
class C:  public A
{  int z;
void fc() { ...x... } //this->x
};
class D: public B, public C
{  int m;
};
D d;
d.fb();
d.fc();
非虚基类
d
x:
y:
x:
z:
m:
sizeof(d) == 5*sizeof(int)

============================================================
=== Slide 18 ===
============================================================

虚基类的一种实现
class A
{  int x;
};
class B: virtual public A
{  int y;
void fb() { ...x... } //this->x
};
class C: virtual public A
{  int z;
void fc() { ...x... } //this->x
};
class D: public B, public C
{  int m;
};
D d;
d.fb();
d.fc();
虚基类
d
？
x,y,z,m:
sizeof(d) != 4*sizeof(int)，为什么？

============================================================
=== Slide 19 ===
============================================================

虚基类的一种实现
class A
{  int x;
};
class B: virtual public A
{  int y;
void fb() { ...x... } //this->x
};
class C: virtual public A
{  int z;
void fc() { ...x... } //this->x
};
class D: public B, public C
{  int m;
};
D d;
d.fb();
d.fc();
虚基类
d
y:
x:
z:
m:
简单去掉一个x，不可行！

============================================================
=== Slide 20 ===
============================================================

虚基类的一种实现
class A
{  int x;
};
class B: virtual public A
{  int y;
void fb() { ...x... } //this->...x，利用虚基类表访问x
};
class C: virtual public A
{  int z;
void fc() { ...x... } //this->...x，利用虚基类表访问x
};
class D: public B, public C
{  int m;
};
D d;
d.fb();
d.fc();
sizeof(d) == 6*sizeof(int)
（假设：指针与int大小相同）
把x移到最后，在原来x的位置存储一个指向x的偏移量指针
d
y:
x:
z:
m:
20
12
D有个虚基类表(vbtable):
存储虚基类的数据成员在D类对象空间中的偏移量

============================================================
=== Slide 21 ===
============================================================

3.6 聚合与组合
智能科学与技术学院
任宇翔

============================================================
=== Slide 22 ===
============================================================

继承不是类代码复用的唯一方式
首先，有些代码复用不宜用继承来实现：
继承除了支持代码复用外，还体现了类之间在概念上的一般与特殊关系（is-a-kind-of）。
如果两个类之间没有这个关系，纯粹是为了代码复用而用继承把它们关联起来，这样会造成概念上的混乱。
例如：“飞机”类复用“发动机”类的功能就不宜用继承来实现。

============================================================
=== Slide 23 ===
============================================================

继承不是类代码复用的唯一方式
其次，继承与封装存在矛盾：
派生类定义新功能或对基类已有成员重定义时往往要用到基类的私有成员。
而从封装的角度，基类的私有成员是不允许外界直接使用的。
再有，继承加大了类之间的耦合度，不利于程序的维护：
基类的一些改变会影响它的派生类。

============================================================
=== Slide 24 ===
============================================================

类之间的整体与部分关系
类之间除了继承关系外，还存在一种整体与部分的关系（is-a-part-of），即一个类的对象包含了另一个类的对象。
例如，“飞机”类与“发动机”类之间就是整体与部分的关系。
在整体与部分的关系中，新的类（整体）通过成员类（部分）复用已有类的功能：
整体类不会用到部分类的私有内容
不会破坏类的封装，也减小了类之间的耦合度。

============================================================
=== Slide 25 ===
============================================================

类之间的整体与部分关系
类之间的整体与部分的关系可以分为：
聚合（aggregation）关系
被包含的对象与包含它的对象独立创建和消亡，被包含的对象可以脱离包含它的对象而存在。
组合（composition）关系
被包含的对象随包含它的对象创建和消亡，被包含的对象不能脱离包含它的对象而存在。

============================================================
=== Slide 26 ===
============================================================

聚合
在聚合关系中，被包含的对象与包含它的对象独立创建和消亡，被包含的对象可以脱离包含它的对象独立存在。例如，
一个公司与它的员工之间是聚合关系。
实现上，
聚合类的成员对象一般是采用对象指针表示，用于指向被包含的成员对象。
被包含的成员对象是在外部创建，然后加入到聚合类对象中。

============================================================
=== Slide 27 ===
============================================================

聚合
class A { ...... };
class B //B与A是聚合关系
{ A *pm; //指向成员对象
public:
B(A *p) { pm = p; } //成员对象在聚合类对象外部创建，然后传入
~B() { pm = NULL; } //传进来的成员对象不再是聚合类对象的成员
......
};
......
A *pa=new A; //创建一个A类对象
B *pb=new B(pa); //创建一个聚合类对象，其成员对象是pa指向的对象
......
delete pb; //聚合类对象消亡了，其成员对象并没有消亡
...... // pa指向的对象还可以用在其它地方
delete pa; //聚合类对象原来的成员对象消亡

============================================================
=== Slide 28 ===
============================================================

聚合
例：公司由一批员工组成
class Employee //职员类
{ string name;
int salary;
public:
Employee(const char *s, int n=0):name(s)
{	salary = n; }
void set_salary(int n) { salary = n; }
int get_salary() const { return salary; }
......
};

============================================================
=== Slide 29 ===
============================================================

聚合
const int MAX_NUM_OF_EMPS=1000;
class Company //公司类
{ String name;
Employee *group[MAX_NUM_OF_EMPS]; //职员数组
int num_of_emps; //职员人数
public:
Company(const char *s):name(s)
{ num_of_emps = 0; }
~Company() { num_of_emps = 0; }
bool add_employee(Employee *e);
bool remove_employee(Employee *e);
int get_num_of_emps() { return num_of_emps; }
......
};

============================================================
=== Slide 30 ===
============================================================

聚合
//创建两个公司类对象
Company c1("Company_1"),c2("Company_2");
//创建两个职员对象
Employee e1("Jack",1000),e2("Jane",2000);
//职员Jack加入公司Company_1
c1.add_employee(&e1);
//职员Jane加入公司Company_1
c1.add_employee(&e2);
//职员Jack从公司Company_1离职
c1.remove_employee(&e1);
//职员Jack加入公司Company_2
c2.add_employee(&e1);
......

============================================================
=== Slide 31 ===
============================================================

组合
在组合关系中，被包含的对象随包含它的对象创建和消亡，被包含的对象不能脱离包含它的对象独立存在。例如，
一个人与他的头、手和脚之间则是组合关系。
实现上，
组合类的成员对象一般直接是对象，有时也可以采用对象指针表示。
但不管是什么表示形式，成员对象一定是在组合类对象内部创建并随着组合类对象的消亡而消亡。

============================================================
=== Slide 32 ===
============================================================

组合
例如，通过成员对象来实现组合：
class A
{ ......
};
class C //C与A是组合关系
{ A a; //成员对象
public:
......
};
......
C *pc=new C; //创建一个组合类对象，其成员对象在组合类对象内部创建
.....
delete pc; //组合类对象与其成员对象都消亡了

============================================================
=== Slide 33 ===
============================================================

组合
组合也可以通过成员对象指针来实现
class A
{ ......
};
class C //C与A是组合关系
{ A *pm; //指向成员对象的指针
public:
C() { pm = new A; } //成员对象随组合类对象在内部创建
~C() { delete pm; pm=NULL; } //成员对象随组合类对象消亡
......
};
......
C *pc=new C; //创建一个组合类对象，其成员对象在组合类对象内部创建
.....
delete pc; //组合类对象与其成员对象都消亡了

============================================================
=== Slide 34 ===
============================================================

组合
用指针表示成员对象时，既可以实现组合关系，也可以实现聚合关系，它们的区别在于：
在组合关系中，成员对象是由包含它的对象创建和撤销。
在聚合关系中，成员对象不由包含它的对象创建和撤销。

============================================================
=== Slide 35 ===
============================================================

例：利用一个线性表类实现一个队列类。
线性表由若干元素构成，元素之间有线性的次序关系。
class LinearList
{		......
public:
bool insert( int x, int pos );
bool remove( int &x,  int pos );
int element( int pos ) const;
int search( int x ) const;
int length( ) const;
};

============================================================
=== Slide 36 ===
============================================================

例：利用一个线性表类实现一个队列类。
队列（Queue）是一种特殊的线性表，对它只能实施两个操作：
en_queue：在线性表的一端插入一个元素
de_queue：在线性表的另一端删除一个元素
又称先进先出表（First In First Out，FIFO）

============================================================
=== Slide 37 ===
============================================================

例：利用一个线性表类实现一个队列类。
Queue的实现1（组合）
class Queue
{		LinearList list; //与队列对象同时创建和消亡
public:
bool en_queue(int i)
{ return list.insert(i,list.length());
}
bool de_queue(int &i)
{ return list.remove(i,1);
}
};

============================================================
=== Slide 38 ===
============================================================

例：利用一个线性表类实现一个队列类。
Queue的实现2（继承）
class Queue: private LinearList
{	public:
bool en_queue(int x)
{  return insert(x,length());
}
bool de_queue(int &x)
{  return remove(x,1);
}
};
这里为什么不用public继承？
实际上，private继承已经退化成组合了！

============================================================
=== Slide 39 ===
============================================================

继承与聚合/组合的比较
在基于继承的代码复用中，一个类向外界提供两种接口：
public：对象（实例）用户
public+protected：派生类用户
在基于聚合/组合的代码复用中，一个类对外只需一个接口：
public：所有用户

============================================================
=== Slide 40 ===
============================================================

继承与聚合/组合的比较
继承的代码复用功能常常可以用组合来实现。

============================================================
=== Slide 41 ===
============================================================

继承与聚合/组合的比较
继承更容易实现子类型：
在C++中，public继承的派生类往往可以看成是基类的子类型。
在需要基类对象的地方可以用派生类对象去替代。
发给基类对象的消息也能发给派生类对象。
具有聚合/组合关系的两个类不具有子类型关系！
继承拥有一个组合永远无法替代的杀手锏：实现子类型与多态

============================================================
=== Slide 42 ===
============================================================

谢谢观看！