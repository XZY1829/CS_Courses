
============================================================
=== Slide 1 ===
============================================================

3.3  虚函数与消息的动态绑定
智能科学与技术学院
任宇翔

============================================================
=== Slide 2 ===
============================================================

消息的多态性
不同类型的对象可以处理相同的消息，但处理方式往往是不一样的，因此，同一条消息可以有不同的解释（处理）。
例如，下面的A和B类都有对消息f的处理函数：
class A
{	public:
void f();
......
};
class B
{	public:
void f();
......
};

============================================================
=== Slide 3 ===
============================================================

消息的静态绑定
一般情况下，在编译时刻根据对象的类型来决定采用哪一个消息处理函数，即采用静态绑定。例如：
class A
{	public:
void f();
......
};
class B
{	public:
void f();
......
};
A a;
B b;
a.f(); //调用A类的消息处理函数f
b.f(); //调用B类的消息处理函数f

============================================================
=== Slide 4 ===
============================================================

消息的动态绑定
在public继承中，由于基类的指针或引用可以指向或引用基类对象，也可以指向或引用派生类对象，这就存在一种特殊的多态性：
如果在基类和派生类中都给出了对某条消息的处理函数，那么，通过基类的指针或引用向它指向或引用的对象发送这条消息时，它会调用哪一个消息处理函数呢？
基类的？
派生类的？

============================================================
=== Slide 5 ===
============================================================

消息的动态绑定
void func1(A& x)
{	......
x.f(); //调用A::f还是B::f ？
......
}
void func2(A *p)
{	......
p->f(); //调用A::f还是B::f ？
......
}
......
A a;
func1(a);
func2(&a);
B b;
func1(b);
func2(&b);

============================================================
=== Slide 6 ===
============================================================

消息的动态绑定
然而，通常情况下，往往需要的是在运行时刻，根据func1（或func2）中x（或p）实际引用（或指向）的对象来决定是调用A::f还是B::f，即采用动态绑定。
在C++中，需要在基类中用虚函数来指出动态绑定。
例如，下面A类中的f是个虚函数，通过A类的指针或引用调用函数f时将采用动态绑定：
class A
{		int x,y;
public:
virtual void f(); //虚函数
};
class B: public A
{		int z;
public:
void f();
void g();
};

============================================================
=== Slide 7 ===
============================================================

消息的动态绑定
void func1(A& x)
{	......
x.f(); //调用A::f或B::f
......
}
void func2(A *p)
{	......
p->f(); //调用A::f或B::f
......
}
......
A a;
func1(a); //在func1中调用A::f
func2(&a); //在func2中调用A::f
B b;
func1(b); //在func1中调用B::f
func2(&b); //在func2中调用B::f

============================================================
=== Slide 8 ===
============================================================

虚函数
虚函数是指加了关键词virtual的成员函数。其格式为：
virtual <成员函数声明>;
虚函数有两个作用：
指定消息采用动态绑定。
指出基类中可以被派生类重定义的成员函数。

============================================================
=== Slide 9 ===
============================================================

在派生类中重定义基类的成员函数
对于基类中的一个虚函数，在派生类中定义的、与之具有相同型构的成员函数是对基类该成员函数的重定义（或称覆盖，override）。
相同型构是指派生类中定义的成员函数：
名字、参数个数和类型与基类相应成员函数相同；
返回值类型与基类成员函数返回值类型或者相同，或者是基类成员函数返回值类型的public派生类。
消息的动态绑定将绑定到派生类中与基类同型构的成员函数！

============================================================
=== Slide 10 ===
============================================================

在派生类中重定义基类的成员函数
class A
{	 int x,y;
public:
virtual void f(int);
};
class B: public A
{	 int z;
public:
void f(int);  //对A中f的重定义
void f(double); //新定义的成员函数
void g();
};
......
A *p=new B;
p->f(1); //调用B::f(int)
p->f(1.0); //double转换成int，调用B::f(int)

============================================================
=== Slide 11 ===
============================================================

在派生类中重定义基类的成员函数
class A
{	 int x,y;
public:
virtual void f(int);
};
class B: public A
{	 int z;
public:
void f(double);
void g();
};
上面B中f的本意是对A中f进行重定义，结果写错了（参数类型写成了double），导致编译器把它看成是一个新定义的函数！
A *p=new B;
p->f(1.0); //调用A::f，而不是调用B::f
可以在派生类中给一个成员函数加上override，告诉编译器：该函数是对基类中某虚函数的重定义，编译器将会去检查基类中有没有同型构的虚函数，如果没有，报错！

============================================================
=== Slide 12 ===
============================================================

在派生类中重定义基类的成员函数
如果在派生类中给出了“完美”的重定义，不需要在之后的派生类中再重定义，可以在重定义时加上final。例如：
class A
{	 int x,y;
public:
virtual void f(int);
};
class B: public A
{	 int z;
public:
void f(int) final; //不允许在之后的派生类中再重定义
void g();
};
class C: public B
{	 ......
public:
void f(int); //Error，不能再重定义！
};

============================================================
=== Slide 13 ===
============================================================

在派生类中重定义基类的成员函数
final还有一个作用：指出不允许被继承的类。例如：
class A final //不允许继承
{	......
};
class B: public A //Error
{	......
};

============================================================
=== Slide 14 ===
============================================================

对虚函数的几点说明
只有类的成员函数才可以是虚函数，但静态成员函数不能是虚函数。
构造函数不能是虚函数，析构函数可以（往往）是虚函数。
只要在基类中说明了虚函数，在派生类、派生类的派生类、...中，与基类同型构的成员函数都是虚函数（virtual可以不写）。
只有通过基类的指针或引用访问基类的虚函数时才进行动态绑定。
基类的构造函数和析构函数中对虚函数的调用不进行动态绑定。（为什么？）

============================================================
=== Slide 15 ===
============================================================

消息动态绑定的各种情况
class A
{  ......
public:
A() { f(); }
~A() { f(); }
virtual void f();
void g();
void h() { f(); g(); }
};
class B: public A
{  ......
public:
B();
~B();
void f();
void g();
};

============================================================
=== Slide 16 ===
============================================================

消息动态绑定的各种情况
class A
{  ......
public:
A() { f(); }
~A() { f(); }
virtual void f();
void g();
void h() { f(); g(); }
};
class B: public A
{  ......
public:
B();
~B();
void f();
void g();
};

============================================================
=== Slide 17 ===
============================================================

通过基类指针访问派生类中新定义的成员
class A
{	      .......
public:
virtual void f();
};
class B: public A
{	      ......
public:
void f();
void g();
};
A *p=new B;
......
p->f(); //OK
p->g(); //Error，通过p只能访问A的成员
((B *)p)->g(); //OK，不安全！万一p指向的不是B类对象呢？

============================================================
=== Slide 18 ===
============================================================

何时需要定义虚函数？
基类中哪些成员函数需要设计成虚函数？
在设计基类时，有时虽然给出了某些成员函数的实现，但实现的方法可能不是最好，今后可能还会有更好的实现方法。
在基类中根本无法给出某些成员函数的实现，它们必须由不同的派生类根据实际情况给出具体的实现。（抽象类与纯虚函数）

============================================================
=== Slide 19 ===
============================================================

虚函数动态绑定的实现
func(&a)
func(&b)
class A
{	int x,y;
public:
virtual void f();
virtual void g();
void h();
};
class B: public A
{	int z;
public:
void f();
void h();
};
void func(A *p) //可以接收A和B类对象
{ p->h(); //编译成：A::h(p);
p->f();  //如何编译？
p->g(); //如何编译？
}

============================================================
=== Slide 20 ===
============================================================

3.4抽象类
智能科学与技术学院
任宇翔

============================================================
=== Slide 21 ===
============================================================

问题
如何表示一组图形对象？（它们的基本功能是一样的，但各自又有一些特殊功能）
class Rectangle
{	......
};
class Line
{	......
};
class Circle
{	......
};
......
? figures[100]; //元素可以是Rectangle、Line或Circle对象

============================================================
=== Slide 22 ===
============================================================

问题
如何让函数func既可以接收ArrayStack类的对象，又可以接收LinkedStack类的对象？（它们的内部实现不一样，但对外接口是一样的）
class ArrayStack
{   int elements[100],top;
public:
ArrayStack() { top = -1; }
void push(int i);
void pop(int& i);
};
class LinkedStack
{   struct Node { int content; Node *next; } *first;
public:
LinkedStack() { first = NULL; }
void push(int i);
void pop(int& i);
};

============================================================
=== Slide 23 ===
============================================================

问题
void func(? st) //可接收ArrayStack和LinkedStack两类对象
{	......
st.push(...);
......
st.pop(...);
......
}

============================================================
=== Slide 24 ===
============================================================

纯虚函数
纯虚函数是没给出实现的虚函数，函数体用“=0”表示， 例如：
class A
{	......
public:
virtual int f()=0; //纯虚函数
......
};
纯虚函数需要在派生类中给出实现。

============================================================
=== Slide 25 ===
============================================================

抽象类
包含纯虚函数的类称为抽象类。例如，下面的A就是一个抽象类：
class A //抽象类
{	  ......
public:
virtual int f()=0; //纯虚函数
......
};
抽象类中可以包含：
纯虚函数：派生类必须给出实现。
虚函数：派生类可以用基类的，也可以自己实现。
非虚函数：派生类只能用基类的实现。

============================================================
=== Slide 26 ===
============================================================

抽象类
注意：抽象类不能用于创建对象！例如，不允许创建下面的A类对象：
A a;  //Error，A是抽象类
抽象类的作用是什么？
对不同类型的公共特征进行抽象描述。除了纯虚函数外，它可以包含普通虚函数和非虚函数。
为同一个类型的不同实现提供一个抽象描述（接口）。它只包含纯虚函数。

============================================================
=== Slide 27 ===
============================================================

抽象类
例：用抽象类为各种图形提供基本框架描述
可以为各种图形对象设计一个抽象基类Figure，它为具体的图形类提供一个基本框架，具体的图形类至少要具有该框架中的功能。
Figure
Rectangle
Circle
Line

============================================================
=== Slide 28 ===
============================================================

抽象类
class Figure //抽象基类
{  int id;
public:
Figure(int i) { id = i; }
//派生类必须实现的功能
virtual void draw() const=0;
virtual void input_data()=0;
//派生类可以自己实现的功能
virtual double area() const { return 0;}
//派生类不能自己实现的功能
int get_id() { return id; }
......
};

============================================================
=== Slide 29 ===
============================================================

抽象类
class Rectangle: public Figure
{		double left,top,right,bottom;
public:
Rectangle(int i): Figure(i) {}
void draw() const
{	...... //画矩形
}
void input_data()
{	cout << "请输入矩形的左上角和右下角坐标 (x1,y1,x2,y2) ：";
cin >> left >> top >> right >> bottom;
}
double area() const //派生类自己实现
{ return (bottom-top)*(right-left);
}
......
};

============================================================
=== Slide 30 ===
============================================================

抽象类
const double PI=3.1416;
class Circle: public Figure
{		double x,y,r;
public:
Circle(int i): Figure(i) {}
void draw() const
{	...... //画圆
}
void input_data()
{	cout << "请输入圆的圆心坐标和半径 (x,y,r) ：";
cin >> x >> y >> r;
}
double area() const //派生类自己实现
{ return r*r*PI; }
......
};

============================================================
=== Slide 31 ===
============================================================

抽象类
class Line: public Figure
{		double x1,y1,x2,y2;
public:
Line(int i): Figure(i) {}
void draw() const
{	...... //画线
}
void input_data()
{	cout << "请输入线段的起点和终点坐标 (x1,y1,x2,y2) ：";
cin >> x1 >> y1 >> x2 >> y2;
}
//派生类没有自己实现area，用基类的
......
};
......
const int MAX_NUM_OF_FIGURES=100;
Figure *figures[MAX_NUM_OF_FIGURES];
int count=0;

============================================================
=== Slide 32 ===
============================================================

抽象类
图形数据的输入：
for (count=0; count<MAX_NUM_OF_FIGURES;	count++)
{	int shape;
do
{	cout << "请输入图形的种类(0：线段，1：矩形，2：圆，-1：结束)：";
cin >> shape;
} while (shape < -1 || shape > 2);
if (shape == -1) break;
switch (shape)
{	case 0: //线
figures[count] = new Line(count);	break;
case 1: //矩形
figures[count] = new Rectangle(count); break;
case 2: //圆
figures[count] = new Circle(count); break;
}
figures[count]->input_data(); //动态绑定到相应类的input_data
}

============================================================
=== Slide 33 ===
============================================================

抽象类
图形的输出：
for (int i=0; i<count; i++)
figures[i]->draw();  //通过动态绑定
//调用相应类的draw
即使今后增加了图形的种类，
上述代码（属于高层代码）不需要改动！
只需要增加相应的类（属于低层代码）就行。
这体现了多态带来的好处：便于高层代码的复用。

============================================================
=== Slide 34 ===
============================================================

抽象类
例：用抽象类实现抽象数据类型Stack
抽象数据类型（abstract data type）是指只考虑类型的抽象性质，而不考虑该类型的实现。
例如，从抽象数据类型的角度来讲，栈类型是由若干具有线性关系的元素构成，它包含两个操作push和pop：
“s.push(a).pop(x)”操作之后，条件“x == a”成立。
上面就是栈这个抽象数据类型所包含的全部内容，它与栈的内部是如何实现的无关！

============================================================
=== Slide 35 ===
============================================================

抽象类
C++的类不是抽象数据类型，它是抽象数据类型的实现！
对于一个用数组实现的栈类ArrayStack和一个用链表实现的栈类LinkedStack：
虽然从抽象数据类型的角度看，它们应该是相同的类型，
但由于它们是用不同的类实现的，因此，在C++中它们是不同的类型！

============================================================
=== Slide 36 ===
============================================================

抽象类
对于需要一个栈对象的函数：
void func(T& st);
如果要求：
它既可以接受ArrayStack类的对象，
也可以接受LinkedStack类的对象，
那么，它的参数T的类型应该怎么表示？
解决方案：
为ArrayStack和LinkedStack提供一个抽象基类Stack，作为它们的对外接口描述：
class Stack
{	public:
virtual void push(int i)=0;
virtual void pop(int& i)=0;
};

============================================================
=== Slide 37 ===
============================================================

抽象类
class ArrayStack: public Stack
{		int elements[100],top;
public:
ArrayStack() { top = -1; }
void push(int i) { ...... }
void pop(int& i) { ...... }
};
class LinkedStack: public Stack
{		struct Node
{	int content;
Node *next;
} *first;
public:
LinkedStack() { first = NULL; }
void push(int i) { ...... }
void pop(int& i) { ...... }
};

============================================================
=== Slide 38 ===
============================================================

抽象类
void func(Stack &st)
{	......
st.push(...);  //将根据st引用的对象类来确定push的归属
......
st.pop(...);  //将根据st引用的对象类来确定pop的归属
......
}
int main()
{	ArrayStack st1;
LinkedStack st2;
func(st1);  //OK
func(st2);  //OK
......
}

============================================================
=== Slide 39 ===
============================================================

用抽象类实现类的真正抽象作用
由于在C++中使用某个类时必须要见到该类的定义，因此，
使用者能够见到该类的一些实现细节（如：非public成员），这样会使得类的抽象和封装作用大打折扣。
有手段绕过类的访问控制而使用类的非public成员，类的封装作用可能被破坏，带来安全问题！

============================================================
=== Slide 40 ===
============================================================

用抽象类实现类的真正抽象作用
例如：
//A.h （类A的定义，公开的）
class A
{	int i,j;
public:
A();
A(int x,int y);
void f(int x);
......
};
//B.cpp （类A的某个使用者）
#include "A.h"
void func(A *p)
{	p->f(2); //Ok
p->i = 1; //Error
p->j = 2; //Error
//可绕过对象类的访问控制！
*((int *)p) = 1; //Ok，访问p所指向的对象的成员i
*((int *)p+1) = 2; //Ok，访问p所指向的对象的成员j
}
如何防止上面的情况？
i:
j:
p:

============================================================
=== Slide 41 ===
============================================================

用抽象类实现类的真正抽象作用
给类A提供一个抽象基类I_A作为对外接口
//I_A.h （类A的对外接口，公开）
class I_A
{ public:
virtual void f(int)=0;
......
};
//B.cpp （类A的某个使用者）
#include "I_A.h"
void func(I_A *p)
{	p->f(2);  //Ok
*((int *)p) = 1;  //这里不知道p所指向的对象有哪些数据成员，
//因此，该操作不知道它访问的是什么数据成员
}

============================================================
=== Slide 42 ===
============================================================

谢谢观看！