# Core — pin versions after first successful run
# Use PyTorch CUDA wheels to avoid silent CPU fallback/downgrade.
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124
torchvision==0.21.0+cu124
torchaudio==2.6.0+cu124
transformers>=4.38.0
datasets>=2.16.0
accelerate>=0.27.0
peft>=0.9.0
bitsandbytes>=0.42.0
sentencepiece>=0.1.99

# Training stack (optional if using LLaMA-Factory submodule)
# llamafactory  # install from upstream repo

# Eval & utils
scikit-learn>=1.3.0
pandas>=2.0.0
matplotlib>=3.8.0
tqdm>=4.66.0
pyyaml>=6.0.0

# Experiment tracking (optional)
# wandb>=0.16.0
