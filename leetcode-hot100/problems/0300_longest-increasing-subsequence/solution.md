# 300. 最长递增子序列 - 题解
## 思路
**贪心 + 二分**：维护 `tails` 数组——`tails[i]` 是长度为 i+1 的递增子序列的最小末尾。对每个元素用 `lower_bound` 找位置替换或追加。
## 复杂度
- **时间**：O(n log n)　**空间**：O(n)

## 关键点
1. `tails` 始终有序，可以用二分
2. DP 法 O(n²) 也可行但面试中 O(n log n) 更优
