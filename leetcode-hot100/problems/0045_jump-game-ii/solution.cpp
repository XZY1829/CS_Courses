// 45. 跳跃游戏 II
#include <iostream>
#include <vector>
#include <algorithm>
#include <cassert>
using namespace std;
class Solution {
public:
    int jump(vector<int>& nums) {
        // TODO: 在此实现
    }

};
int main() {
    Solution sol;
    vector<int> n1 = {2,3,1,1,4}; assert(sol.jump(n1) == 2);
    vector<int> n2 = {2,3,0,1,4}; assert(sol.jump(n2) == 2);
    cout << "All tests passed!" << endl;
}
